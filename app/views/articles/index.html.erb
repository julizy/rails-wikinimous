<ul>
  <% @articles.each do |article| %>
    <div>
    <li class = 'list'><h2><%= link_to article.title, article_path(article), class:'title' %></h2></li>
    <p><%= article.content %></p>
    </div>
  <% end %>
</ul>
